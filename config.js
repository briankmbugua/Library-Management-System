const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "password",
        database: "library_db",
        connectTimeout: 6000
    },
    listPerPage: 10,
    secretKey: 'brian'

};

module.exports = config;